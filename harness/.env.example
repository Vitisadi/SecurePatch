# Copy this file to `.env` (same directory) and fill in your keys.
# The harness loads it automatically at CLI startup (see __main__._load_env).
# `.env` is gitignored; this template is committed. Real environment variables
# take precedence over anything set here.

ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
