"""SecurePatch research harness.

Week 1 scope: a thin bridge to the @securepatch/core detection CLI plus an
append-only JSONL result writer. The experiment runner, provider adapters, and
verification loop are added in later weeks (see docs/RESEARCH_PLAN.md).
"""

__version__ = "0.0.1"
